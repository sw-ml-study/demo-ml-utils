Create the initial thin repository validation workflow. Add a justfile that delegates to scripts; deterministic license, documentation-link, fixture, and source checks; documented MLPL binary selection that uses MLPL or an adjacent development build without installation; and the initial demo/catalog schema. Use TDD for executable behavior, run the full check gate, update user documentation, commit named files including AgentRail metadata before agentrail complete, then stop.