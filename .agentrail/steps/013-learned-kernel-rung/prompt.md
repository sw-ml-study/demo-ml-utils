Build rung 6 of demos/cnn: recover a convolution kernel from examples by making the rung 4 contraction differentiable. Use grad through windows and the broadcast multiply with adam. Show both a successful recovery and a failure: on a linear ramp the loss reaches machine precision while the learned kernel is wrong, because neighbouring receptive fields are linearly dependent and the problem is underdetermined; on random input the solution is unique and the teacher kernel is recovered. State that training is not part of Equation (1). Verify the gradient against central differences rather than trusting that it runs.