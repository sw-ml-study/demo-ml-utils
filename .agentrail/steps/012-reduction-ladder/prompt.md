Build rungs 1-3 of demos/cnn: dot product, 1-D convolution, and 2-D convolution. Each rung shows the same computation three times: the source equation, the mechanical transliteration with one nested reduce per summation, and the array-oriented collapse. Carry the equation in an @formula annotation and print it alongside the implementation so the correspondence is the demo's own output; use Unicode and bracket-index notation in terminal output, never LaTeX. Assert the hand-checked -6 upper-left cell from docs/research.txt. Follow TDD, keep functions small and pure, document every function, and format with mlpl-fmt before committing. Acceptance: each rung is runnable via a thin justfile recipe, output narrates scenario and interpretation rather than ending at PASS, and goldens are hand-derived rather than captured from the implementation.