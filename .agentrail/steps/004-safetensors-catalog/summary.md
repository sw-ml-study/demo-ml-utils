BLOCKED: Configured mlpl-repl 0.20.0 commit cce9874e cannot enumerate keys of a parsed record: only has_field and record_get are exposed. A Safetensors catalog must discover arbitrary tensor names. The JSON decoder also inserts object members into a BTreeMap without rejecting duplicate keys, so duplicates are lost before MLPL can validate them. Hard-coding names or delegating substantive parsing to Rust would violate this step's MLPL implementation contract. Minimal unblock: expose deterministic record_keys(record) -> string-list and make parse_json reject duplicate object keys (or provide an equivalently general budgeted JSON member iterator), then add executable conformance probes.