Pin the array-expressiveness capability surface for the convolution-from-equations demo. Add catalog entries for the five blocker probes (probes/rank-broadcast.mlpl, multi-axis-reduce.mlpl, named-axis-reduce.mlpl, sliding-windows.mlpl, compress-label-preservation.mlpl) and probes/convolution-reference.mlpl so the default gate reports which of C1-C5 are shipped. Re-pin docs/capabilities.md to the observed build commit. Record the measured cost of the C2 kernel-replication workaround and the runtime gap against native conv2d. Capability claims pin build commits, never version strings: see docs/sw-mlpl-blocker.md for why. Acceptance: the gate reports probe state, docs/sw-mlpl-blocker.md and docs/sagas.md agree with the observed binary, and no claim rests on a version number.