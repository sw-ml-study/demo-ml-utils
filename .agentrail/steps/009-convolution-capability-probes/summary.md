Pinned the array-expressiveness capability surface. Added catalog/probes.tsv and scripts/check-capability-probes, wired into the default gate and exposed as 'just array-capabilities'; probe expectations now fail closed in both directions. Observed C1 and C5 shipped, C2/C3/C4 open on build f4485823. Corrected this repository's C2 cost analysis: the 172x gap and 1,036,800 replicated cells described a table-tiling workaround, not the language. The im2col matmul spelling needs no broadcasting, runs at 1.198ms against conv2d's 1.230ms, and is exact; C2 demoted to ERGONOMICS_ONLY with no rung gated on it. Made im2col parity executable in probes/convolution-reference.mlpl. Re-pinned docs/capabilities.md and recorded the settled demos/cnn ownership boundary.