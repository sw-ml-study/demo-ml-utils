Consolidate acceptance for the convolution-from-equations demo. Cover adversarial shapes, window larger than the axis, empty and single-element inputs, budget failures, and non-finite values, each failing closed with a named rejection. Confirm repeated runs are byte-identical. Update catalog/demos.tsv, README.md, docs/capabilities.md, and the demo catalog in the same step. Record attribution for Zhao, Wang, Wang and Liu (2018), stating that only the section 2.1 definition is implemented and not the paper's faster algorithm. State limitations explicitly: teaching scale, forward-only, measured performance gap against conv2d. Apply the reconciliation rule from docs/sw-mlpl-blocker.md by rewriting each rung for whichever of C2, C3, and C4 have shipped, retaining the superseded spelling as a documented exhibit rather than deleting it. Decide and record whether trainable convolution (upstream Phase 5 autograd) and the live-site rendering boundary (upstream Phase 6) become separate sagas.