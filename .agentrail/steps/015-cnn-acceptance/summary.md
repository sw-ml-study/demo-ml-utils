Wrote docs/cnn-acceptance.md consolidating the convolution demo evidence: what each of the six rungs proves, the oracle policy (exact on integers, a stated 1e-9 tolerance on floats because chained reductions accumulate differently from one fused builtin), adversarial and boundary cases with named rejections including window-equals-input as a success, byte-identical determinism, and the C1-C6 capability trail. C4 is recorded as still open but closed downstream as a library gap. Attribution states that only Section 2.1 Equation (1) is implemented and the paper's Winograd/Strassen contribution is not, and notes the equation was verified by reading the paper rather than the transcript. Limitations cover teaching scale, forward and single-kernel training only, fixed stride and padding, cross-correlation rather than flipped-kernel convolution, and the conditional nature of rung 6's recovery. Also recorded the withdrawn 172x performance claim. Updated the docs index, README, saga status, and corrected the plan's stale five-rung count.