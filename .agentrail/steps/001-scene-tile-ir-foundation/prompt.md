Implement the first model-visualization vertical slice using TDD: define a versioned renderer-neutral JSON scene/tile IR in MLPL over the repository's accepted bounded artifact summaries. Specify stable IDs, format/tensor provenance, nodes or tiles, scalar attributes, links where justified, and explicit object, payload-byte, depth, element, label, and output budgets enforced before construction or serialization. Reuse existing Safetensors summary IR conventions where sound, but define a cross-format contract that can represent both Safetensors and GGUF without reading complete tensor payloads. Add generated golden and adversarial fixtures, deterministic JSON round-trip/headless validation, and malformed/budget failure cases. Add a self-describing just demo explaining the artifact inputs, schema, budgets, representative output, and interpretation; include it in the default presentation gate. Reconcile the demo catalog, authoritative attribution, complexity and actual allocation/copy behavior, limitations, plan and saga docs. Run the full gate, commit and push implementation before agentrail complete, then define the tensor-city layout step, commit and push only generated completion metadata, and stop.