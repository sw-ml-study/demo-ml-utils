Implement renderer-neutral Q8_0 quantization-block and before/after error tiles using TDD. Use the generated GGUF golden block and an explicit bounded reference/original vector to expose block scale, signed quants, dequantized values, reference values, pointwise errors, and aggregate size/RMSE/max-error/cosine metrics in MLPL. Preserve stable parent tensor IDs and provenance and assign an explicit LOD. Enforce catalog, dtype, payload, block, parameter, reference, metric, point, object, coordinate/value, label, iteration, JSON depth/element, and encoded-output budgets before I/O, expansion, construction, or serialization. Define zero-vector cosine and finite-value policies. Add byte-level golden/oracle evidence, metric golden vectors, malformed/budget adversarial cases, deterministic tagged JSON headless validation, and a self-describing just demo explaining the quantized block, scale/quants, reconstruction errors, budgets, and output interpretation; include it in the default presentation gate. Reconcile schema docs, demo catalog, attribution, complexity and actual allocation/copy behavior, limitations, plan and saga status. Run the full gate, commit and push implementation before agentrail complete, then queue the transport/optional-client acceptance step, commit and push only generated completion metadata, and stop.