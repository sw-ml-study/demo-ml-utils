Implemented and shipped deterministic atomic Safetensors extraction from approved restricted-checkpoint tensor ranges. Added dynamic JSON-escaped tensor header entries, exact little-endian header length encoding, eight-byte space padding, I8/I16 payload assembly, per/total storage and output budgets, write_atomic replacement, and independent existing catalog/selective-decode self-validation with exact source/output value parity. The generated 2x2 I16 fixture produces an 80-byte artifact and decodes [1,-2,300,512]; tests cover GLOBAL/persistence/schema/range failures, header/storage/output budgets, and preservation of an existing destination on pre-write failure. Added a self-describing just checkpoint-to-safetensors demo, official-format documentation, catalog/README/plan/saga wiring, and demo-output validation. Full just check passed. Adjacent selected binary reported 91d5216a rather than the user-noted c3452aa1, but existing required capabilities were sufficient; packed u8 and extension array boundaries remain unclaimed. Commit b83a996 was pushed to main.