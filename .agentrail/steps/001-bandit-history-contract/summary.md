Defined and documented a bounded deterministic bandit task/history contract for the ICRL saga. Added two disjoint source tasks and one held-out task with exact reward tapes, action/observation/reward histories, cumulative reward and regret, deterministic fixed-greedy and UCB1 baselines, and explicit programmed-baseline/non-ICRL labels. Added schema, reward, task-separation, arm/horizon/comparison/history/output budgets, deterministic tagged JSON, adversarial leakage/resource tests, a self-describing demo, repository integration, and passed the full just check gate.