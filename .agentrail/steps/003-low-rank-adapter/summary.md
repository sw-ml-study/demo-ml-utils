Implemented and documented a bounded frozen-base low-rank adaptation demo with rank-one training, rank-two shape support, manual factor gradients, unchanged-base fingerprints, merged/factorized inference parity, trainable and stored parameter accounting, convergence trajectory, held-out evidence, and explicit production-LoRA limitations. Registered the demo and passed the full just check gate.