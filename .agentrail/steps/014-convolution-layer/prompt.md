Build rung 5 of demos/cnn: a full convolution layer, Y = relu(W * X + b), so the reader sees that conv2d is not magic once window, multiply, and reduce are understood. Add bias and activation to the rung 4 contraction. Include pool2d agreement only if the pooling shape arithmetic can be stated honestly; if it cannot, say so and leave it out rather than approximating. State memory complexity and measure a high-water bound, per the repository rule for any demo claiming bounded behavior. Report the measured runtime gap against native conv2d in the demo output rather than hiding it; this is a teaching path, not an implementation. Acceptance: layer output verified against the oracle, budgets enforced and failing closed, and the performance gap stated in user-visible output.