Shipped leakage-safe pure-MLPL Tune: training fits slope 2, validation visibly scores offsets [0,1,2] as [1,0,1], deterministically selects offset 1, and the selected downstream predictor is evaluated exactly once on untouched held-out rows for MSE 0. A separate tie case proves lowest-index selection. Added identity leakage, empty/excess candidate, malformed/non-finite, and resource rejection; no upstream blocker was found. Full just check passes.