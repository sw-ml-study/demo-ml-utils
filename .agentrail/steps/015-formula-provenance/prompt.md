Make the mathematics durable and renderable without LaTeX in terminal output. Verify the @formula annotation round-trips through annotations() and prints beside the implementation it describes. Emit the formula string as a field in the renderer-neutral scene IR and have viewer/transport-viewer.html typeset it, reusing the existing scene-IR transport rather than inventing a new one; upstream sw-mlpl already ships an MLPL-expression-to-LaTeX converter feeding MathJax for its derivation view, so do not write a second one. Do not check pre-rendered equation images into docs: this repository's demos are self-checking, and a static image is the one artifact that cannot be verified against the code it describes. Acceptance: one source of truth for each equation, terminal output legible without LaTeX, and the IR round-trip validated headlessly.