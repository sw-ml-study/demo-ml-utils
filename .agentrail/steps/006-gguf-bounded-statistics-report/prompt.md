Complete the gguf-inspection saga with bounded selected-tensor sampling/statistics and a measured acceptance report. Use TDD and build on exact-name catalog resolution plus the shipped I8/I16 and Q8_0 decoders. Design chunk- or block-bounded mergeable statistics that do not materialize an arbitrarily large tensor, with explicit read, block, sample, output, parameter, and iteration budgets enforced before I/O or expansion. Add generated golden and adversarial fixtures, verify merges against direct small-fixture results, and include an opt-in sparse-artifact peak-RSS acceptance path if supported without downloads. Add a self-describing just demo that explains the artifact, selected ranges or blocks, intermediate/merged statistics, budgets, and output meaning; include default presentation validation. Reconcile the demo catalog, authoritative sources, attribution, complexity, actual allocation/copy behavior, limitations, plan and saga status; write the GGUF acceptance report and make an evidence-based visualization-saga gate decision. Run the full gate, commit and push implementation before agentrail complete. Then mark the saga done, commit and push only generated completion metadata, and stop.