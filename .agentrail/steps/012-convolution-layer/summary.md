Rung 5 of demos/cnn is built and runnable: a full convolution layer, Y = relu(W * X + b). The pre-activation matches the conv2d oracle plus bias exactly, relu is shown zeroing 12 negative cells, and pool2d halves each spatial axis. The demo states explicitly that Equation (1) contains no bias and no activation, so this rung goes beyond the cited definition rather than implying the paper defines a layer that way. Delivered in commit 18a5957 before this step was opened.