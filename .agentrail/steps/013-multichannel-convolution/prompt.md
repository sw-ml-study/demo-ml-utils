Build rung 4 of demos/cnn: the Zhao, Wang, Wang and Liu (2018) section 2.1 multi-channel triple sum, y[r,x,y] = sum_q sum_u sum_v W[r,q,u,v] X[q,x+u,y+v]. Use the native windows builtin, whose axis order is frozen at [out_y, out_x, C, kh, kw] so a [channel, kernel_y, kernel_x] kernel aligns by trailing position with no transpose_axes. Check against the native conv2d builtin as an independent oracle, not as an implementation: exact on integer inputs, and inside a stated tolerance on floats because accumulation order differs. Until C2 ships, the kernel multiply uses the documented one-line table-tiling workaround marked as the C2 placeholder; do not defer this rung to a reduce-only patch sum, and do not claim the tiling line is free. Add the labeled-axis version. Acceptance: conv2d parity evidence, an explicit float tolerance policy, and attribution naming which part of the paper is used.