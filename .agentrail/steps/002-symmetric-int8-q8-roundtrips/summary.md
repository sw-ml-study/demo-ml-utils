Implemented and pushed symmetric INT8 and GGUF-compatible Q8_0 round trips in cc6f4dd: bounded max-absolute scaling, half-away nearest rounding, saturation, signed-byte packing, finite IEEE binary16 scale encoding, exact 34-byte Q8_0 goldens, existing independent decoder acceptance, zero blocks, reconstruction metrics, adversarial budgets, narrated demo, docs, and green full gate.