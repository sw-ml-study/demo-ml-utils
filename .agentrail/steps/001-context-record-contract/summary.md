Defined and documented a bounded versioned ICL context/query record over the shared deterministic fixture. Added full source-split plus context/query leakage validation, stable IDs, example/element/comparison/numeric/JSON/output budgets, unchanged before/after deployment fingerprints, zero deployment updates, and an honest zero-shot baseline that ignores context labels. Added deterministic tagged-JSON and adversarial budget tests, a self-describing demo, repository integration, and passed the full just check gate.