Complete the model-visualization saga with a bounded CLI/server transport contract and optional 3D client path while retaining headless acceptance as authoritative. Define deterministic JSON/JSONL envelopes for scene, tensor-city, detail, and quantization-error IR with schema/version, message kind, sequence, provenance, payload-byte/object/LOD budgets, and fail-closed decode validation. Implement the substantive envelope/validation logic in MLPL; any server/browser/native transport must remain explicit and optional. Add golden ordering/round-trip and malformed/budget adversarial tests, a self-describing default just demo that explains message flow and outputs, and an opt-in client or static viewer only if unblocked by current capabilities without hidden dependencies. Write the final visualization acceptance report with attribution, complexity, copy/allocation behavior, limitations, and evidence-based next-saga recommendation; reconcile catalog, plan, and saga status. Run full gate, commit and push implementation, mark saga done with agentrail complete --done, then commit/push only generated completion metadata and stop.