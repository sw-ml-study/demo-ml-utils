Implement selective bounded GGUF tensor reads and golden decoding for a deliberately small unquantized dtype set, building on the multi-tensor catalog. Use TDD. Resolve tensor names exactly from the catalog, derive absolute payload offsets from aligned tensor_data, enforce dtype/rank/parameter/read budgets before payload I/O, and decode selected little-endian values in MLPL without reading unrelated tensors. Start with types whose exact extent rules are already cataloged and add signed/unsigned or floating support only with exact golden conversion evidence. Add generated payload fixtures and adversarial cases, a self-describing just demo following the narrative contract, default-gate presentation validation, and reconcile catalog/docs/complexity/limitations. Run the full gate, commit and push before agentrail complete, then persist generated completion metadata and stop.