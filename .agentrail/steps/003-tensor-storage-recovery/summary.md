Implemented and shipped declarative tensor metadata/storage-reference recovery over the restricted primitive graph. Added validated multi-member ZIP cataloging and a strict five-field descriptor schema (storage, dtype, shape, offset, length), safe unique tensor names, I8/I16 width arithmetic, positive bounded shapes, exact byte-length checks, and non-data.pkl member-range validation while reading zero storage bytes and resolving no globals, callables, constructors, extensions, or persistence. Added deterministic valid, dangerous-persistence, bad-dtype, bad-length, and missing-storage fixtures; golden/adversarial/budget/round-trip tests; and a narrated just checkpoint-tensor-metadata demo showing the recovered 2x2 I16 tensor, absolute range, opcode/report evidence, and safety counters. Full just check passed. Product commit 46c073c was pushed to main.