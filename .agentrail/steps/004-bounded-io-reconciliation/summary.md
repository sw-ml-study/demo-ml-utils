Reconciled demo-ml-utils with live sw-MLPL commit cce9874e: executable probes now cover bounded read_bytes, file_size, EOF/missing-path behavior, max_elements with max_depth/max_bytes, and duplicate-key rejection. Safetensors header inspection now uses file_size plus two budgeted reads, making memory independent of model size. The remaining catalog blocker is deterministic record-key enumeration. just check passes; commit 50c5b0c is pushed.