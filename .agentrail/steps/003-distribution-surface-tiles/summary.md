Implemented renderer-neutral distribution/surface schema version 1 over strictly bounded selected Safetensors integer and GGUF Q8_0 tensors. MLPL derives stable parent IDs, LOD 2, explicit empty/constant/range policies, four deterministic equal-width histogram bins, mergeable counts, and endpoint-inclusive sampled surface strips. Enforced underlying catalog/dtype/parameter budgets plus payload, block, decoded sample, bin, surface point, object, iteration, displayed-value, output, JSON depth, and element limits. Golden tests prove cross-format outputs, histogram partition merge parity, sample indices/order, deterministic tagged JSON, and fail-closed budgets. Added a narrated just detail-tiles default-gate demo and documented attribution, O(P+B+S+J) work, actual bounded full-selection materialization, limitations, plan and saga status. Full just check passes; implementation commit 40e7def was pushed to main.