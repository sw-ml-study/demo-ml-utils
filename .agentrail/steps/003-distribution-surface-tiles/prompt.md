Implement selected-tensor distribution and sampled-surface tiles over the bounded scene/tensor-city foundation using TDD. Support the accepted Safetensors I8/I16 path and GGUF I8/I16/Q8_0 path without materializing arbitrarily large tensors. Define deterministic histogram/distribution bins and a renderer-neutral sampled surface or strip representation with stable parent tensor IDs, provenance, LOD level, sample coordinates/values, and explicit empty/constant/range policies. Enforce catalog, dtype, payload chunk, block, parameter, sample, bin, surface point, iteration, object, coordinate/value, label, JSON depth/element, and encoded-output budgets before I/O, expansion, construction, or serialization. Add cross-format golden fixtures, merge/chunk parity, sampling/order invariants, malformed/budget adversarial cases, and deterministic tagged JSON headless validation. Add a self-describing just demo explaining the selected tensors, histogram/surface construction, budgets, representative bins/points, and interpretation; include it in the default presentation gate. Reconcile schema docs, demo catalog, attribution, complexity and actual allocation/copy behavior, limitations, plan and saga status. Run the full gate, commit and push implementation before agentrail complete, then queue quantization-block and before/after error tiles, commit and push only generated completion metadata, and stop.