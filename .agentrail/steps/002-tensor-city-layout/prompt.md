Implement deterministic tensor-city model/layer layout over scene/tile IR version 1 using TDD. Generalize the bounded tile collection only as needed to represent multiple catalog entries while preserving stable IDs and provenance. Derive explicit hierarchy/grouping from tensor names with a documented deterministic policy, then assign renderer-neutral positions, extents, heights, and scalar attributes in MLPL. Enforce catalog, object, hierarchy depth, coordinate, label, link, payload, iteration, JSON depth/element, and encoded-output budgets before construction or serialization. Add golden layouts for both Safetensors and GGUF fixtures, ordering and collision invariants, malformed/budget adversarial cases, and headless deterministic JSON validation. Add a self-describing just demo that explains the model artifacts, grouping/layout policy, budgets, representative coordinates, and output meaning; include it in the default presentation gate. Reconcile schema docs, demo catalog, attribution, complexity and copy/allocation behavior, limitations, plan and saga status. Run the full gate, commit and push implementation before agentrail complete, then queue selected-tensor distribution/sampled-surface tiles, commit and push only generated completion metadata, and stop.