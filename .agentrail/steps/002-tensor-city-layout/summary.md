Implemented deterministic cross-format tensor-city schema version 1 in MLPL over complete bounded Safetensors and GGUF catalogs without payload reads. The renderer-neutral columnar IR retains arbitrary padded UTF-8 names and stable artifact:tensor:name IDs, exact collision checks, format districts, dot-segment hierarchy depths, deterministic x/y/z positions, unit footprints, parameter-derived heights, and parameter/byte attributes. Enforced catalog, logical object, tile, zero-link, hierarchy, coordinate, text-width, iteration, conservative/actual output, JSON depth, and decode-element budgets. Golden/adversarial tests pin both catalog orders, geometry, hierarchy, attributes, IDs, deterministic tagged JSON, malformed IDs, and budget failures. Added a narrated just tensor-city default-gate demo and reconciled schema, complexity/copy behavior, limitations, plan and saga docs. Full just check passes; implementation commit 7b38807 was pushed to main.