Implement the first bounded GGUF v3 catalog vertical slice using TDD. Pin and cite the authoritative GGUF format contract in user-facing documentation. Generate tiny redistributable valid and malformed fixtures locally. Parse magic, version, tensor_count, metadata_count, a deliberately conservative scalar metadata subset, alignment, and tensor descriptors without reading tensor payloads. Enforce byte/count/string/rank/exact-integer/file-bound budgets before allocation or iteration; reject unsupported metadata types and malformed offsets fail closed. Keep parsing and validation in MLPL, native code only for generic file_size and bounded read_bytes. Add deterministic golden tests, a runnable catalog demo/default-gate integration, catalog and roadmap reconciliation, and honest complexity/allocation limits. Run the full gate, commit and push source plus in-progress Agentrail metadata before agentrail complete; then persist generated completion metadata and stop.