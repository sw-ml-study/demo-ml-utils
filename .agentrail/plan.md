1. Define a bounded context/query record contract with deterministic splits, leakage checks, frozen parameter fingerprints, and a zero-shot baseline. 2. Implement fixed-parameter associative attention or nearest-example ICL with visible one-shot and few-shot evidence. 3. Add context controls for order, distractors, contradictions, truncation, and leakage. 4. Build a renderer-neutral comparison IR for context composition, attention, predictions, and accuracy. 5. Add deterministic native and adversarial ICL acceptance, optional pinned external-model corroboration, attribution, limitations, and saga closure.