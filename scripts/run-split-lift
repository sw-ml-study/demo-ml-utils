#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd); mlpl=$("$repo_root/scripts/select-mlpl")
output=$($mlpl --source-dir "$repo_root" "$repo_root/tests/split-lift.mlpl"); printf '%s\n' "$output" | grep -Fqx 'PASS bounded Split/Lift baseline'
$mlpl --source-dir "$repo_root" "$repo_root/demos/experiments/split_lift.mlpl"
