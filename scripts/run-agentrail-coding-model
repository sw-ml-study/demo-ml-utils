#!/bin/sh
set -eu
echo "SKIP: Stage 2 training needs the planned Rust-native sw-MLPL causal-LM provider." >&2
echo "Missing: tokenizer, Qwen loader, LoRA optimizer, adapter persistence, and generation APIs." >&2
exit 77
