Demonstrated and documented held-out reward-context adaptation using the source-trained frozen distilled policy. After bounded explore-each-arm actions, growing reward features [0,0,1,2] raise action-one probability from 0.5 to 0.93 and 0.994, yielding actions [0,1,1,1], reward 3, and regret 1. Empty and reward-ablated contexts fall to reward 1/regret 3; shuffled context is invariant for the cumulative feature. Added exact unchanged fingerprints, zero deployment updates, zero held-out offline records, deterministic and round/output budget tests, a self-describing demo, repository integration, and passed the full just check gate.