Closed the bounded ICRL saga with deterministic and adversarial held-out acceptance plus a versioned renderer-neutral history IR. Canonical and empty/shuffled/reward-ablated variants carry aligned actions, rewards, reward-context features, probabilities, context lengths, cumulative reward, and regret. Acceptance proves byte-identical reproduction, source/held-out separation, unchanged deployment fingerprints and zero updates; rejects malformed/non-finite data and exhausted resources; atomically publishes exact bytes and validates parsed semantics while preserving destinations on pre-write failure. Documented attribution, optional non-authoritative external corroboration, limitations, and passed the full just check gate.