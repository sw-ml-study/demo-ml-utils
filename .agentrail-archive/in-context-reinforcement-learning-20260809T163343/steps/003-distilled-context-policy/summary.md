Trained and documented a miniature history-conditioned policy offline from source bandit histories, then published a versioned frozen artifact for later held-out rollout. Manual BCE gradient descent reduces loss from 0.693 to 0.0389 in 40 updates and learns a positive reward-difference weight with correct negative/positive action decisions. Added before/after fingerprints, exact zero held-out training records, deterministic tagged JSON, step/curve budgets, explicit offline-not-ICRL attribution, a self-describing demo, repository integration, and passed the full just check gate.