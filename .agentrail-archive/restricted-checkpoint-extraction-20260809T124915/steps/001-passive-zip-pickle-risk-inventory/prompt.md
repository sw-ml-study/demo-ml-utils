Using TDD, implement a passive bounded inventory for tiny generated PyTorch-style ZIP checkpoints and contained pickle bytecode without importing torch, invoking pickle deserialization, or executing any opcode. Pin authoritative ZIP and Python pickle format/security references. Generate redistributable deterministic benign and adversarial fixtures without Python in the default gate if practical. Inventory ZIP magic/member metadata and pickle protocol/opcode counts, classify allow-listed declarative versus dangerous executable/global/reduce/constructor/extension/persistence opcodes, enforce artifact/member/opcode/string/memo/depth/iteration/output budgets before allocation or iteration, and produce a versioned risk report. Reject malformed/truncated/duplicate/path-traversal/encrypted/compressed-unsupported members and unknown/dangerous opcode policy as appropriate, but keep inventory passive. Add golden/adversarial tests, a genuinely self-describing just demo explaining its evidence, catalog/docs with MLPL/native attribution, logical complexity and actual copy behavior, full gate, commit/push, then complete. Do not modify ../sw-mlpl.