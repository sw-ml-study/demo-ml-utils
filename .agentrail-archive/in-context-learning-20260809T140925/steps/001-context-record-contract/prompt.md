Using TDD, define a versioned bounded context/query record schema for in-context learning. Reuse the shared adaptation dataset and terminology where relevant; enforce train/context/evaluation separation and leakage checks, context/query/example and numeric budgets, an unchanged deployment-parameter fingerprint, and a meaningful zero-shot baseline. Add deterministic tagged JSON, a self-describing demo, documentation, and repository gate integration.