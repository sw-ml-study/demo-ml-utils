Implemented exact bounded GGUF Q8_0 decoding in MLPL. The catalog now validates complete 32-value blocks and exact 34-byte extents; selective decode resolves exact tensor names and enforces dtype, read, block, parameter, and output budgets before one payload read. Binary16 scale conversion rejects non-finite values, signed quants match ggml dequantize_row_q8_0 golden semantics, and generated malformed fixtures cover partial blocks. Added a narrated just gguf-q8-0 demo, default presentation gating, authoritative attribution, complexity/memory/limitations documentation, and reconciled plan/saga status. Full just check passes; implementation commit 2779351 was pushed to main.