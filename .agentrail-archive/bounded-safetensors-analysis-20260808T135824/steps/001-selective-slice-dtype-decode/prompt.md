Implement selective Safetensors tensor-region reads and golden decoding for an intentionally small byte-aligned dtype set. Use TDD. Validate tensor lookup, dtype support, element alignment, exact offsets, file bounds, and a caller-supplied read budget before I/O. Keep format-specific decoding in MLPL and generic bounded read_bytes/file_size native. Add deterministic tiny fixtures and success/failure tests, a runnable demo or extension, catalog and user-facing attribution/complexity documentation, and thin just/script integration. Run the full repository gate. Commit and push source plus in-progress Agentrail metadata before agentrail complete; then persist generated completion metadata in a second commit and stop.