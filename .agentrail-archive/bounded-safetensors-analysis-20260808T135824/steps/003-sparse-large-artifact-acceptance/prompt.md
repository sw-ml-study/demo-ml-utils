Add an opt-in sparse large-Safetensors acceptance test for the fixed-chunk statistics path. Generate the artifact locally without downloading models or committing a large payload, make its logical size exceed a documented memory budget, run bounded statistics with deterministic expected results, and measure process peak resident memory using a portable documented harness where feasible. Establish an evidence-backed high-water threshold that distinguishes chunk-bound behavior from whole-tensor materialization. Keep the default gate fast and tiny; add an explicit just recipe, prerequisites, cleanup, and skip/failure semantics. Reconcile catalog, plan, sagas, and complexity docs. Run the default gate plus the opt-in acceptance, commit and push before agentrail complete, then persist completion metadata and stop.