Implement mergeable Safetensors tensor statistics over repeated fixed-size bounded reads. Use TDD and build on the selective integer decoder without materializing the complete tensor. Define and test mergeable count, min, max, sum, mean, and numerically stable variance state; prove chunked results match single-pass golden values and merges across partitions. Enforce chunk and output budgets before I/O, keep reductions in MLPL, add deterministic demo/default-gate integration, update catalog and complexity/allocation documentation, run the full gate, commit and push before agentrail complete, then persist completion metadata and stop.