Built and documented a versioned renderer-neutral adaptation evidence IR that normalizes full and low-rank fine-tuning into shared curve, update, prediction, baseline, and provenance channels. Added headless structural/alignment validation, canonical step coordinates, deterministic byte-identical tagged JSON rebuild and round-trip evidence, explicit curve/output/JSON budgets, adversarial rejection checks, and a self-describing runnable demo. Registered it throughout the repository and passed the full just check gate.