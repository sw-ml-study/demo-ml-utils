Implemented and shipped the shared bounded adaptation contract for fine-tuning, ICL, and ICRL. Added a generated two-feature dataset with four train, two labeled-context, and three evaluation examples; strict root/split/shape/finite/magnitude budgets; within-split duplicate rejection; 26 explicit cross-split leakage comparisons; and total work limits. Defined executable deployment invariants: fine-tuning requires parameter updates, ICL requires labeled context with zero deployment updates, and ICRL requires reward history with zero deployment updates. Added canonical tagged-JSON parameter fingerprints and executable goldens for dot, matmul, stable row-wise softmax, sigmoid, exp/log, and deterministic seeded random. Added round-trip and adversarial/budget tests, fixture drift generation, a self-describing just adaptation-contract demo, and README/catalog/capability/plan/saga documentation. Full just check passed. Commit 7944fa9 was pushed to main.