Using TDD, demonstrate Ensemble with visibly different member predictions, disagreement, deterministic aggregation, and metric comparison; then demonstrate Feed with an upstream prediction becoming a downstream feature, visible intermediate values, and a meaningful ablation. Preserve the common computation/evaluation path where current MLPL permits it, otherwise publish a minimized classified blocker.