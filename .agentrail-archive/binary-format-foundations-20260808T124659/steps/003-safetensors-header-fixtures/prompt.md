Generate tiny redistributable Safetensors fixtures covering valid, empty, truncated, oversized, malformed, overflow, and boundary headers. Implement and test an MLPL little-endian 8-byte header-length decoder within current exactness limits. Keep fixture generation deterministic, run just check, document current whole-file memory behavior, commit before agentrail complete, then stop.