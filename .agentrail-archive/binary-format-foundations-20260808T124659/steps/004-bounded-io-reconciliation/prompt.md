Reconcile demo-ml-utils with the newly deployed live sw-MLPL capabilities. Add executable probes for file_size, bounded read_bytes(path, offset, length), EOF/error behavior, and parse_json max_elements alongside max_depth/max_bytes. Refactor Safetensors header inspection to read only the 8-byte prefix and declared bounded header rather than materializing the whole file. Update stale capability, plan, catalog, fixture, and development documentation without claiming the still-blocked arbitrary tensor catalog. Run just check, commit and push named files before agentrail complete, then stop.