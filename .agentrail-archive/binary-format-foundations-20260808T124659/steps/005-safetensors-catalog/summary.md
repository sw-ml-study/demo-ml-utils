Implemented the bounded arbitrary-name Safetensors catalog on live sw-MLPL commit 6156e869. record_keys discovery is probed and sorted; MLPL validates exact tensor schema, string metadata, supported byte-aligned dtypes, shapes, offsets, full non-overlapping data coverage, bounds, and aggregate counts. Ten new deterministic catalog fixtures cover valid mixed tensors and adversarial failures. just check passes; commit 7053fc2 is pushed.